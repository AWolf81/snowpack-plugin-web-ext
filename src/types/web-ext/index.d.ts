declare module 'web-ext'; /*{
  type WebExtConfig = {
    run?: {
      target: Array<string>;
    };
    sourceDir?: string;
  };
  declare interface cmd {
    run(config: WebExtConfig, options: WebExtConfig);
    build(config: WebExtConfig, options: WebExtConfig);
  }

  declare const WebExt: cmd;
  export default WebExt;
}*/
